<h1>Listing users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_image_tag user.email, :size => 30 %>
      <%= link_to user.firstname + " " + user.name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>